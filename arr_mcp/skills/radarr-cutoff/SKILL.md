---
name: radarr-cutoff
description: "Generated skill for Cutoff operations. Contains 1 tools."
---

### Overview
This skill handles operations related to Cutoff.

### Available Tools
- `get_wanted_cutoff`: No description
  - **Parameters**:
    - `page` (int)
    - `pageSize` (int)
    - `sortKey` (str)
    - `sortDirection` (str)
    - `monitored` (bool)
    - `radarr_base_url` (str)
    - `radarr_api_key` (Optional[str])
    - `radarr_verify` (bool)

### Usage Instructions
1. Review the tool available in this skill.
2. Call the tool with the required parameters.

### Error Handling
- Ensure all required parameters are provided.
- Check return values for error messages.
